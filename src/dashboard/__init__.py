"""Shared pure chart/table builders used by the static site (src/site/*).

- `charts` : pure builders (DataFrames, plotly figures) — no Streamlit, unit-tested;
  the only submodule in this package.
"""
