# Learning log — finance history

A running record of what we've covered, so the `/finance` tutor resumes the thread instead of
restarting. One line per session: date · covered · understood / gap · next.

---

- **2026-06-04 · setup** — Seeded the knowledge base: 9 eras (dotcom → higher-for-longer) with real
  history, the `src/eras.py` index, the 28-year metrics timeline (1998→now), and era bands on the
  Trends charts. **Next:** start with the **GFC** (the clearest "credit-is-the-line" lesson), then walk
  the causal chain forward (GFC → ZIRP → COVID → inflation → today). Run `/finance gfc` to begin.
